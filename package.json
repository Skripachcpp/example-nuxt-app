{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "dp": "tsc scripts/dp.mts --skipLibCheck --allowSyntheticDefaultImports --module ESNext --target ESNext --outDir ./dist --moduleResolution node && node dist/dp.mjs",
    "css": "tsc scripts/css.mts --skipLibCheck --allowSyntheticDefaultImports --module ESNext --target ESNext --outDir ./dist --moduleResolution node && node dist/css.mjs",
    "hi": "node nuxt-harmony"
  },
  "dependencies": {
    "@nuxt/eslint": "^1.7.0",
    "@nuxt/test-utils": "^3.19.2",
    "eslint": "^9.31.0",
    "nuxt": "^4.0.1",
    "nuxt-harmony": "^1.0.5",
    "vue": "^3.5.17",
    "vue-router": "^4.5.1"
  }
}
